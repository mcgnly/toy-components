class Zookeeper {
    constructor(greeting) {
        this.greeting = greeting;
    }
    says(greeting) {
        //     if (!(this.greeting)) {
        //         throw new Error('tell him what to say');
        //     }

        //     return new Promise(function(resolve, reject) {
        //         util.ajax({
        //             url: 'something.com/' + this.greeting,
        //             type: 'GET'
        //         }, function(response) {
        //             resolve(response);
        //         }, function(error) {
        //             reject(error);
        //         });
        //     });
        // }
        // util: {
        //     ajax(url) {
        //         //go to the url and get back (greeting+", world")
        //     }
    }
}

export
default Zookeeper